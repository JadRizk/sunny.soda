import { RemNumber } from '../utils/RemValue';

export type FontSizes = {
  scale01: RemNumber;
  scale02: RemNumber;
  scale03: RemNumber;
  scale04: RemNumber;
  scale05: RemNumber;
  scale06: RemNumber;
  scale07: RemNumber;
  scale08: RemNumber;
  scale09: RemNumber;
};

export const fontSizes: FontSizes = {
  scale01: '1.2rem',
  scale02: '1.4rem',
  scale03: '1.6rem',
  scale04: '1.8rem',
  scale05: '2.4rem',
  scale06: '3rem',
  scale07: '3.8rem',
  scale08: '4.6rem',
  scale09: '5.6rem',
};
